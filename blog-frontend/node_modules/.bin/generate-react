#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../generate-react-cli/bin/generate-react" "$@"
  ret=$?
else 
  node  "$basedir/../generate-react-cli/bin/generate-react" "$@"
  ret=$?
fi
exit $ret
